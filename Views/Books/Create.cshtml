@{
    ViewData["Title"] = "Create Book";
}

@model LibraryMvc.Models.Book

<h1>Add a New Book</h1>
<hr>
@using (Html.BeginForm())
{
    <input name="BookId" type="hidden" value="@ViewBag.BookId">

    <!-- Not positive regarding AuthorId -->
    @*<input name="AuthorId" type="hidden" value="@ViewBag.AuthorId">*@
    
    @Html.LabelFor(model => model.Title)<br>
            <input data-val="true"  required id="Title" name="Title" type="text" value=""><hr>

    @Html.LabelFor(model => model.Genre)<br>
    <input data-val="true"  required id="Genre" name="Genre" type="text" value=""><hr>

    @Html.Label("Add an Author")<br>
    @Html.DropDownList("AuthorId")

   

        <input type="submit" value="Add New Book" class="btn btn-outline-success"/>
        <br><br>
        @Html.ActionLink("Cancel", "Details", "Authors", new { id = @ViewBag.AuthorId })
}


